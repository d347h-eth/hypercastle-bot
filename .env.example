# Remote sales feed API (base URL only; /sales/v6 will be appended)
SALES_API_BASE_URL=
SALES_API_KEY=
SALES_COLLECTION_ADDRESS=

# X (Twitter) API - OAuth 1.0a user context (recommended simplest)
X_APP_KEY=
X_APP_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_SECRET=
# Use fake publisher (logs locally) instead of posting to X (set to "true" to enable fake)
USE_FAKE_PUBLISHER=false

# X user (for optional timeline verification)
X_USER_ID=
X_USERNAME=

# Polling / Rate limiting
POLL_INTERVAL_MS=30000
RATE_LIMIT_MAX_PER_DAY=17
# UTC hour when the daily window resets (0-23)
RATE_LIMIT_RESET_HOUR_UTC=0

# Database
DB_PATH=./data/bot.sqlite.db

# Templating (multi-line example)
TWEET_TEMPLATE="#{tokenId} | {name} | {price} {symbol} (take-{orderSide})\n{Mode} {Chroma}{Antenna}\n{Zone} B{Biome}"
